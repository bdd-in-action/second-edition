#!/usr/bin/env node

require('ts-node/register')

const { flyingHighApp } = require('../src')

flyingHighApp.listen(3000, 'localhost', () => {
    console.log('Flying High listening on port 3000')
})
